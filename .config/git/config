[user]
	useConfigOnly = true
[credential]
	helper = cache
[alias]
	hub = !git config user.name Lina && git config user.email 36717206+linabeee@users.noreply.github.com && git config --local --get-regexp user
	lab = !git config user.name Lina && git config user.email 4982999-flatulation@users.noreply.gitlab.com && git config --local --get-regexp user
	ci = commit
	st = status
	diff = diff ':!package-lock.json' ':!yarn.lock' ':!pnpm-lock.yaml'
[pull]
	rebase = false
[init]
	defaultBranch = main
