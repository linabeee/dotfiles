[user]
	useConfigOnly = true
	name = Lina
	email = 36717206+lina-bh@users.noreply.github.com
[credential]
	helper = cache
[alias]
	hub = !git config user.name lina && git config user.email 36717206+lina-bh@users.noreply.github.com && git config --local --get-regexp user
	uni = !git config user.name 'Lina Bhaile (001234998)' && git config user.email lb2894y@gre.ac.uk && git config --local --get-regexp user
	emacs-devel = !git config user.name 'Lina Bhaile' && git config user.email emacs-devel@linabee.uk && git config --local --get-regexp user
	ci = commit
	st = status
	c = !git commit --m '' --allow-empty-message
        upsub = !git submodule update --init --recursive
[init]
	defaultBranch = main
[advice]
	addIgnoredFile = true
